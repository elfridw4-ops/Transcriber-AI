import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.videotranscriber.app',
  appName: 'VideoTranscriber',
  webDir: 'dist'
};

export default config;
